<main class="content">
    <h1>Desafío 1.1</h1>
    <div class="content-resource">
        <div class="rs-container">    
            <div class="statement">
                <p>
                    Encuentre dónde \( f \) tiene máximo y mínimo absolutos. Halle los valores máximo y mínimo absolutos de \( f \).\[
        f(x) = \left( x^2 - 1 \right)^3, \quad x \in [-1, 2].
        \]
                </p>
            </div>

            <div class="challenge-answer">
                <form action="">
                    <input type="text" name="answer" id="" placeholder="Ingresa tu respuesta" class="answer-inp">
                    <input type="submit" value="Enviar" class="submit-btn">
                </form>
            </div>
            <div class="feedback">
                <div class="feedback-statement wrong">
                    <p>
                        La respuesta correcta es:
                    </p>
                    <p>
                        \( h(x) = x^3 \), \( g(x) = x^2 - 1 \); \( f(x) = h(g(x)) \)

\[
f'(x) = h'(g(x)) \cdot g'(x) = 3 \left( x^2 - 1 \right)^2 \cdot 2x
\]
                    </p>
                    <p>Revisa el siguiente video</p>
                </div>

                <div class="feedback-statement correct">
                    <p>
                        ¡Correcto! La respuesta es
                    </p>
                    <p>
                        \( h(x) = x^3 \), \( g(x) = x^2 - 1 \); \( f(x) = h(g(x)) \)

\[
f'(x) = h'(g(x)) \cdot g'(x) = 3 \left( x^2 - 1 \right)^2 \cdot 2x
\]
                    </p>
                </div>

                <div class="feedback-video">
                    <video src="videos/pruebavideo.mov"></video>
                </div>
            </div>
            <button class="next-step">Siguiente</button>
        </div>

        <div class="navigation-theme-container">
            <div class="navigation-table">
                <div class="theme-title">
                    <h5>Tema 01: Valores Máximos y Mínimos</h5>
                </div>
                <div class="theme-resource-container">
                    <div class="theme-resource finished">
                        <button>Video01</button>
                    </div>
                    <div class="theme-resource active">
                        <button>Video01</button>
                    </div>
                    <div class="theme-resource">
                        <button>Video01</button>
                    </div>
                    <div class="theme-resource">
                        <button>Video01</button>
                    </div>
                </div>
                <div class="navigation-btn-container">
                    <button class="navigation-btn">
                        Siguiente
                    </button>
                </div>
            </div>
        </div>
    </div>
</main>      